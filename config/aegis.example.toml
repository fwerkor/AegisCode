[agent]
max_steps = 8
workspace = ".work"
stop_on_approval_required = true

[feedback]
commands = ["python -m py_compile hello.py"]
run_after_actions = ["write_file", "shell", "test"]

[memory]
path = ".aegis/memory.json"
max_results = 5

[guardrails]
require_approval = true
