# aegiscode

Node wrapper for the AegisCode CLI.

```bash
npm i -g aegiscode
aegiscode --help
```

The wrapper delegates to the Python package entry point. For the full runtime, install `aegiscode` with `pipx` or `uv tool`.
